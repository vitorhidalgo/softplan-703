const INITIAL_STATE = require('../../data/gabinete.json');

export default function cards(state = INITIAL_STATE.cards, action) {
    switch(action.type) {
        default:
            return state;
    }
}